// PocketBase — будет подключён позже
export async function createBooking(_data: {
  name: string
  phone: string
  service?: string
  message?: string
}): Promise<void> {
  // TODO: подключить PocketBase
}
